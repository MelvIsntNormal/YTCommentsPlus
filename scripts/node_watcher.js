'use strict';

const global = self;

(function (CommentsPlus) {

  const NodeWatcher = CommentsPlus.NodeWatcher = {
    new: function (inputSelector, outputSelector) {
      const self = Object.create(this);



      return self;
    }
  };

})(global.CommentsPlus = global.CommentsPlus || {});